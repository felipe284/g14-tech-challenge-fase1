# g14-tech-challenge-fase1

# 🚀 Foodlink API — Guia de Configuração e Execução

Este guia descreve como configurar e executar o projeto **Foodlink API** utilizando:

- Java 21
- Spring Boot
- JPA (Hibernate)
- PostgreSQL
- Docker

---

# 📦 Pré-requisitos

Antes de iniciar, certifique-se de ter instalado:

- Java 21
- Maven
- Docker
- Docker Compose

Verifique:

```bash
java -version
mvn -version
docker -v
docker-compose -v
